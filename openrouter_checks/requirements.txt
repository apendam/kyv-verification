requests>=2.31
python-dotenv>=1.0
# Deliberately unpinned upper bound: gradio 4.44.1 (the last release
# compatible with Python <3.10) only declares loose lower-bound
# dependencies (fastapi<1.0, pydantic>=2.0, jinja2<4.0, ...) and breaks
# against today's fastapi/pydantic/jinja2/huggingface_hub releases —
# missing HfFolder, "unhashable type: dict" in template rendering,
# TypeError in gradio_client's schema walker. None of that happens on a
# current gradio (5.x/6.x, needs Python >=3.10) — its own pins track the
# modern stack. So: run this on Python 3.10+ and let pip resolve the
# latest gradio, rather than pinning gradio<5.0 to "fix" an unrelated
# problem, which just forces the broken old combination back in.
gradio>=4.0
Pillow>=10.0
# Local perceptual hashing (pHash) for the duplicate check -- deterministic,
# free (no API call), no per-model threshold-calibration guesswork.
ImageHash>=4.3

# Pure-logic VRN/maker matching, reused from the sibling package instead of
# re-deriving fuzzy-match rules here. Installed separately (see README) since
# it's a local path dependency, not a PyPI package:
#   pip install -e ../truck_extract_match
