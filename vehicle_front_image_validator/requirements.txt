anthropic>=0.40
pillow>=10
pydantic>=2

# NOTE: torch 2.2.2 is the last Intel-macOS build and needs numpy<2. Relax this pin
# on a GPU box with a modern torch.
numpy>=1.26,<2

# Real-model backends (Q1 vehicle/pose gate, Q2 plate colour, Q3 make classifier).
ultralytics>=8.2      # YOLOv8 vehicle detector (Q1)
torch>=2.2            # SigLIP 2 + (optional, unwired-live) Qwen2.5-VL
transformers>=4.49,<5 # SigLIP 2 zero-shot pose/make (Q1/Q3) + Qwen2.5-VL class (Q3, code-only)
                      # NOTE: transformers 5.x requires torch>=2.4; pinned <5 to match
                      # torch 2.2.2 (last Intel-macOS build), same as truck_front_extractor
boto3>=1.34           # AWS Rekognition VRN + text detection (Q2)

# Experimentation backends (test/inference interface — pick CV/LLM per stage).
# None of these have credentials configured in this environment; each degrades
# gracefully ("not configured") the same way backends/qwen.py does until keys are added.
google-genai>=0.3       # Gemini 2.5 Pro/Flash (Q1/Q2/Q3 alternate VLM)
google-cloud-vision>=3.7  # Logo Detection (Q3 make, alternate)
requests>=2.31          # Clarifai REST API (Q3 make, alternate) — no official SDK dependency

# Test/inference interface.
gradio>=4.30           # individual + bulk-CSV test UI (matches truck_verification_pipeline's own choice)
pandas>=2.0            # bulk CSV parsing/result table

# truck_extract_match (VRN grammar + confusion-aware matching, Q2) is a local sibling
# repo, not on PyPI — install it separately (kept out of this file: a `-e file://` line
# here breaks pyproject.toml's dynamic-dependencies parsing for `pip install -e .`):
#   pip install -e ~/truck_extract_match
