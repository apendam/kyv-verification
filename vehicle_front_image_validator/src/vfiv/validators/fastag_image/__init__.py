"""Checks that run against the uploaded FASTAG sticker photo: QR/barcode decode
+ printed-digit OCR, cross-checked against each other and the claimed value —
see ``fastag_check.py``. Not wired into ``validators/combined.py``."""
