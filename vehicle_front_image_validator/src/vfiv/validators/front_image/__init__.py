"""Checks that run against the uploaded FRONT photo: Q1 (front_image.py, the
gate), Q2 (vrn_check.py, VRN + plate colour), Q3 (make_model_check.py, make +
model). Combined into one decision by ``validators/combined.py``; run
individually (with backend selection) via ``experiments/``."""
